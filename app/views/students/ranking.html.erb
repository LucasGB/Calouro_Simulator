<table>
  <tr>
    <th>Usuario</th>
    <th>Nome</th>
    <th>Idade</th>
    <th>Sexo</th>
    <th>Curso</th>
    <th>Coeficiente</th>
    <th colspan="2"></th>
  </tr>
 
  <% @students.each do |student| %>
    <tr>
      <td><%= student.user.email %></td>
      <td><%= student.name %></td>
      <td><%= student.age %></td>
      <td><%= student.gender %></td>
      <td><%= student.bachelor_degree %></td>
      <td><%= student.coefficient %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Voltar', root_path %>